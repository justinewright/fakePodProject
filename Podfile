# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'fakePodProject' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for fakePodProject

  target 'fakePodProjectTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'fakePodProjectUITests' do
    # Pods for testing
  pod 'TutSKImageExtension'
  end

end
